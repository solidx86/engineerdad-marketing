# reel-9x16-chart-line-emphasis-001

**Tone:** Authoritative / data-driven, light (`--offwhite`) background.
**Demonstrates:** A 9:16 Reel chart card that carries a single argument. Line chart from `compounding-30y.yaml` — the 8% hero series is full `--orange` and visually dominant; the 4% comparison and 0% floor lines are dimmed (low-opacity navy / dashed muted) so the eye lands on the divergence. An orange callout badge over the year-30 split names the gap (+RM239K = 447K − 208K, both from the YAML).
**Why it's a good exemplar:** Shows the reel safe-area discipline (top 14% / bottom 20% / right 12% clear), logo pulled up into the safe zone via flow layout (never the IG-button corner), and the full untruncated `source_citation` / PHS compliance line kept legible at ≥24px.
