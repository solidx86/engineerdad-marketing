# reel-9x16-chart-bar-emphasis-002

**Tone:** Alert / high-contrast, dark (`--navy-dark`) background.
**Demonstrates:** A 9:16 Reel bar card on dark. Bar chart from `dca-vs-lump.yaml` — the DCA bar is full hero `--orange`, the lump-sum bar is dimmed translucent white, so the emphasized outcome reads instantly. White ticks/grid per the chartjs-config dark-bg note; logo uses the allowed dark variant (`.engineer` white, `.dad` orange). Callout names the emphasized value (13,043 units), numbers match the YAML exactly.
**Why it's a good exemplar:** Proves chart legibility and contrast on `--navy-dark` at thumbnail scale, single-bar emphasis, and the full `source_citation` surviving on a dark surface inside the reel safe-area.
