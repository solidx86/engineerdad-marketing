---
description: Spawn the brain reasoner directly with a free-form intent. A generic escape hatch — for scoped loop work use /loop or the stage commands.
argument-hint: "<intent>"
allowed-tools: Task
---

Spawn the `brain` subagent (`subagent_type=brain`) with the following message verbatim:

$ARGUMENTS
